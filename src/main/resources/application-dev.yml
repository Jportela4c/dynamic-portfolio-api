# Dev profile overrides - TESTING ONLY
spring:
  security:
    oauth2:
      authorizationserver:
        client:
          portfolio-web-app:
            registration:
              authorization-grant-types:
                - authorization_code
                - refresh_token
                - password  # DEV ONLY: Insecure password grant for testing

# Security Configuration
security:
  admin:
    enabled: true  # Enable ADMIN role for testing
